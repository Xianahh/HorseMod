-- local function blockRightClick(x, y)
--     Mouse.UIBlockButtonDown(1)              -- swallow RMB so the player never sees it
-- end

-- local function resetAim(player)
--     if player:isAiming() then
--         player:setIsAiming(false)           -- immediately drop out of aim
--     end
-- end

-- Events.OnRightMouseDown.Add(blockRightClick)
-- Events.OnPlayerUpdate.Add(resetAim)